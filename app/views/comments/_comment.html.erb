      <li class="list-group-item">

        <div class="media">
          <div class="media-body">
            <h5 class="mt-0">
              <%= link_to "@#{comment.commenter.username}" %>
              <!-- still need to make the link to profile pages work -->
            </h5>
            <p><%= comment.body %></p>
            <p class="font-weight-lighter">Posted <%= time_ago_in_words(comment.created_at) %> ago</p>
          </div>
        </div>

      </li>
